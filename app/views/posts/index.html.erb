
<%= link_to "Settings", edit_user_path(current_user) %>

<h1>Showing all posts</h1>

	<% @posts.each do |p| %>

	<div>
		<%=  p.content %>
		<%= link_to "Edit", edit_post_path(p) %>
		<%= link_to "Delete", p, :method => :delete %><br />
	</div>

	<% end %>
	
	<p></p>
	<p></p>

	<div>
		<%= link_to "New", new_post_path %>
	</div>

